# Web client origin, e.g. http://localhost:3001 during development
CORS_ORIGIN=

# Optional explicit client base URL for callback construction; falls back to CORS_ORIGIN
CLIENT_URL=

# Secret used to derive AES-GCM key for encrypting user API keys
# Use a strong random value (32+ chars). Never expose to the client.
ENCRYPTION_SECRET=

# Better Auth (unused in this flow, kept for future integration)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=